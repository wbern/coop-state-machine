{
    "type": "object",
    "properties": {
        "type": {
            "type": "string",
            "enum": ["build", "invest", "move"]
        }
    },
    "required": ["type"],
    "additionalProperties": true
}
