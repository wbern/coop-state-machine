{
    "type": "object",
    "properties": {
        "action": {
            "type": "string",
            "enum": ["build", "invest", "move", "skip"]
        }
    },
    "required": ["action"],
    "additionalProperties": true
}
