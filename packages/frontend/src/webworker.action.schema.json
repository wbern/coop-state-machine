{
    "type": "object",
    "additionalProperties": true,

    "required": ["action"],

    "properties": {
        "comment": {
            "type": "string"
        }
    },

    "oneOf": [
        {
            "properties": {
                "action": { "enum": ["build"] }
            },
            "required": ["x", "y"]
        },
        {
            "properties": {
                "action": { "enum": ["skip"] }
            }
        }
    ]
}
